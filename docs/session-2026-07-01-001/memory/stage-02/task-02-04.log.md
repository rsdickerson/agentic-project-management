---
stage: 2
task: 4
title: Facts & Trivia
agent: facts-agent
status: Success
important_findings: false
compatibility_issues: false
---

# Task 2.4 - Facts & Trivia

## Summary

Implemented seven fact-style live content modules with unit tests; all 21 tests pass offline with mocked HTTP.

## Details

1. Created modules `fact`, `activity`, `dog`, `cat`, `trivia`, `number`, and `advice` under `src/hello/`, each following the standard pattern: module-level URL constant, unavailable placeholder, separate fetch/format functions, and `fetch_<name>_line()` public API.
2. Used `hello.http.fetch_json` for all endpoints; `cat` uses `user_agent=True`; `trivia` decodes HTML entities with `html.unescape`.
3. Dog module parses nested JSON at `data[0].attributes.body` per dogapi.dog response shape.
4. Wrote one test file per module with success, network failure, and malformed payload cases; tests patch `urllib.request.urlopen` and route by `request.full_url`.

## Output

- `src/hello/fact.py`, `activity.py`, `dog.py`, `cat.py`, `trivia.py`, `number.py`, `advice.py`
- `tests/test_fact.py`, `test_activity.py`, `test_dog.py`, `test_cat.py`, `test_trivia.py`, `test_number.py`, `test_advice.py`

## Validation

- `pytest tests/test_fact.py tests/test_activity.py tests/test_dog.py tests/test_cat.py tests/test_trivia.py tests/test_number.py tests/test_advice.py` — 21 passed
- Each module returns `{Label}: {content}` on success or `{Label}: unavailable` on failure
- Trivia HTML entity decoding verified in `test_fetch_trivia_line_success`
- Cat custom User-Agent verified in `test_fetch_cat_line_success`

## Issues

None
